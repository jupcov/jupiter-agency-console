# Jupiter Agency Console (Claude / Supabase)

Static Agency Console HTML. Talks to Supabase Edge Functions with a dashboard key pasted in the browser.

Separate from `jupiter-ams-wrap` (Chief / NowCerts wrap).
